# Проект "Реестр плановых проверок"

## 📋 О проекте

Веб-приложение для ведения реестра плановых проверок субъектов малого предпринимательства (СМП) контролирующими органами.

### Технологический стек:
- **Бэкенд:** PHP 8.5 + CodeIgniter 4 + PostgreSQL
- **Фронтенд:** Angular 18 + TypeScript

---

## 🚀 Быстрый старт

### Предварительные требования
- PHP 8.5+
- PostgreSQL 14+
- Composer
- Node.js 20+
- npm 10+

🔧 Установка и запуск (Бэкенд)
1. Установка Composer
bash
php -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"
php composer-setup.php
php -r "unlink('composer-setup.php');"
2. Установка зависимостей
bash
# Перейти в папку проекта
cd testing/smp_server

# Удалить старые зависимости (если есть)
rm -rf vendor
rm -f composer.lock

# Установить зависимости заново
composer install
3. Создание базы данных
bash
# Подключиться к PostgreSQL
psql -U postgres

# Выполнить SQL-запросы
CREATE DATABASE smp_db;
CREATE USER ci_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE smp_db TO ci_user;
\q
4. Настройка подключения к БД
Отредактировать файл app/Config/Database.php:

php
public $default = [
    'hostname' => 'localhost',
    'username' => 'ci_user',
    'password' => 'your_password',
    'database' => 'smp_db',
    'DBDriver' => 'Postgre',
    'port'     => 5432,
];
5. Выполнение миграций
bash
php spark migrate
6. Запуск сервера
bash
php spark serve
# Сервер будет доступен по адресу: http://localhost:8080
🎨 Установка и запуск (Фронтенд)
1. Установка зависимостей
bash
# Перейти в папку фронтенда
cd smp_front

# Установить зависимости
npm install
2. Запуск в режиме разработки
bash
npm start
# Приложение будет доступно по адресу: http://localhost:4200
3. Сборка для продакшена
bash
npm run build
# Собранные файлы будут в папке dist/
